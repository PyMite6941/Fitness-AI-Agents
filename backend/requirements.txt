# LIGHT backend deps — the data API (Supabase + Clerk auth). This is what Vercel
# installs. The AI crew (crewai/litellm/etc.) is NOT here: /analyze lazy-imports
# bots.py, and the heavy stack lives in requirements-crew.txt. Keeping this light
# is what lets the backend run on Vercel (always-on, free) under the size limit.
fastapi==0.136.1
uvicorn==0.47.0
python-dotenv==1.2.2
python-multipart==0.0.20
httpx==0.28.1
PyJWT==2.12.1
supabase==2.30.1
